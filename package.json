{
  "name": "blog-post-backend",
  "version": "1.0.0",
  "description": "Test Project for Blogging",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:js && npm run build:css && npm run build:docs",
  "build:js": "babel src -d dist",
  "build:css": "sass src/styles:dist/styles",
  "build:docs": "swagger-cli bundle src/swagger.yaml -o dist/swagger.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.3.3",
    "sequelize": "^6.24.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-dist": "^5.4.2",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.5.1"
  }
}
